# Bongsu Requirements Audit

Updated: 2026-06-04 17:01:00 KST

This audit keeps the original product requirements tied to implementation evidence, verification commands, and remaining gaps. It is not a completion claim; the goal remains open until every item has authoritative evidence from code, tests, deployment, and live behavior.

## Status Legend

- `Verified`: covered by committed implementation and an automated verification command.
- `Implemented`: code exists, but the evidence is narrower than the full commercial requirement.
- `Partial`: useful implementation exists, but known product or verification gaps remain.
- `Open`: no sufficient current evidence.

## Requirement Matrix

| ID | Requirement | Current Status | Evidence | Verification |
| --- | --- | --- | --- | --- |
| R1 | Build and continuously update a security database every 6 hours, with import/export for air-gapped transfer. | Verified | `scripts/sync-all-cvedb.sh`, `scripts/export-security-db-bundle.sh`, `scripts/import-security-db-bundle.sh`, `deploy/docker-compose.yml`, `deploy/docker-compose.airgap.yml`, `docs/architecture.md`; `/api/health` exposes persisted `security_db_freshness.latest_source`, `latest_last_update`, and `latest_age_seconds` so operators can distinguish a restarted sync manager from an empty or failed CVE DB; `/api/admin/security-db/status` returns warnings and recommended actions for stale, missing, failed, or timed-out source checks | `./scripts/verify-deploy-config.sh`, compose config checks, `go test ./internal/server/api` |
| R2 | Classify security sources by OS package vs code package/library and match to compatible sources only. | Verified | `docs/vulnerability-matching-rules.md`, `internal/server/db`, `migrations/022_cve_affected_packages.sql`, package ecosystem fields in API models, focused CVE matching invariant verifier for same-name OS/library collisions and ecosystem aliases, executable rematch test that rejects same-name wrong-ecosystem candidates | `go test ./...`, `go test ./internal/server/db`, `./scripts/verify-cve-matching-invariants.sh` |
| R3 | Collect installed OS/library packages, host metadata, running containers, images, and relationship context, then send SBOM to the server. | Implemented | `internal/agent/collector`, `cmd/agent`, `internal/agent/reporter`, `internal/shared/models`, server-side scan report asset-context normalization for host/container packages and vulnerabilities, DB package/container/user/process/port persistence, latest-scan host runtime inventory APIs for users, top processes, and listening ports with RBAC scope, live operator workflow verifier that reports deterministic users/processes/ports and then reads them through the host runtime inventory APIs, executable latest-SBOM package query test preserving container/image/target ontology, SBOM export paths in server API, CycloneDX/SPDX ontology preservation tests, bounded agent scan controls for host scan root, host Trivy timeout, per-container timeout, container skip, and max container count, live verifier agent report ingestion tied to scan request completion, real agent binary workflow verifier with fixture Trivy/osquery/docker collection for two logical hosts, host OS packages, host npm libraries, container OS packages, container PyPI libraries, osquery packages, purl/ecosystem preservation, and container inventories, live host-token binding verifier, live RBAC fixture ingestion of distinct allowed/denied hosts and containers | `go test ./...`, `go test ./internal/server/api`, `go test ./internal/server/db`, `./scripts/verify-operator-workflow.sh`, `./scripts/verify-agent-binary-workflow.sh`, `./scripts/verify-live-agent-token-binding.sh`, `./scripts/verify-live-rbac-scope.sh`; real fleet deployment fixture remains useful |
| R4 | RBAC by UserID/GroupID for host/container/system access. | Verified | access subject/policy migrations, local admin sessions accepted for admin APIs, viewer-key flow, `/api/admin/rbac-*` handlers, `/api/admin/rbac/status` operational counters for subjects, policies, wildcard policies, orphan policies, resource types, permissions, and subject types, dashboard RBAC view, dynamic asset-group scope expansion, source-level scope enforcement tests for host/container/image/asset-group policies, live RBAC scope verifier for two-host/two-container viewer-key filtering across hosts, packages, containers, scans, and scan requests via an `asset_group` policy plus fail-closed denied-host filters | `go test ./...`, `npm --prefix web run test:e2e`, `./scripts/verify-operator-workflow.sh`, `./scripts/verify-live-rbac-scope.sh`; broader multi-tenant staging fixtures remain useful |
| R5 | Provide a sufficiently good web interface. | Verified | Vite dashboard, first-screen `bongsu`/`봉수대` product intro, CVE DB status card, persisted latest OSV/source update display, admin security DB and agent fleet warnings with recommended operator actions, CVE Search, vulnerability/host/RBAC/audit/admin views, host detail runtime inventory panels for users, top processes, and listening ports, username/password login through the `5678` web proxy, schedules, asset groups, trends, reports, notifications, optional-widget failure tolerance, browser workflow coverage for schedules, asset groups, reports, notifications, and operational warning rendering; fixed schedule and asset-group UI/API contract mismatches; live browser verifier for dashboard, CVE Search, Hosts, Packages, Containers, Scan History, Vulnerabilities, RBAC, Audit Log, Schedules, Asset Groups, Trends, Reports, and Notifications routes plus product intro text | `npm --prefix web run build`, `npm --prefix web run test:e2e`, `./scripts/verify-live-web-smoke.sh` |
| R6 | Deploy fully in air-gapped environments; update externally, export, import internally. | Verified | airgap compose file, bundle import/export, package script, packaged docs, static binary verification, trivy archive validation, package contents verifier, package smoke verifier, offline package rehearsal, generated release archive verifier for outer/inner checksums, required contents, executable/static binaries, Docker image archives for server/web/agent/postgres, loader, and airgap compose invariants; real `scripts/package.sh` run produced `bongsu-0.1.0-real-20260604055407.tar.gz` (208M, SHA256 `ff200255584ecc524a1cf6daf387c5a5ab47dfe65a8b804c5bfef44ba45d7900`), archive verification and offline rehearsal passed, the packaged loader loaded server/agent/web/postgres images, packaged compose started on isolated ports `5687/5688`, and a 109M exported security DB bundle imported successfully with `806272` CVE rows, `682665` affected-package rows, `1308724` reference keys, and 100% affected/reference index coverage | `./scripts/verify-deploy-config.sh`, `./scripts/verify-package-contents.sh`, `./scripts/verify-airgap-package-smoke.sh`, `./scripts/verify-airgap-release-archive.sh <archive>`, `./scripts/verify-airgap-offline-rehearsal.sh <archive>`, `./scripts/verify-static-binaries.sh`, compose config checks, isolated package load/compose/import rehearsal |
| R7 | Provide force scan and rematch functions. | Implemented | scan request APIs, dashboard force-scan buttons, asset-group scan trigger, scheduled scans, agent daemon polling/retry, CVE rematch endpoints, executable CVE rematch test for compatible matchable candidates, executable stale-rematch cleanup test for removing obsolete `cve-db` findings only, auto-rescan on security DB changes, executable DB test for security DB update queue accounting/dedupe/revision propagation, live operator workflow verifier for schedule CRUD, asset-group scan trigger, agent claim, agent report, and scan-request completion, real agent binary daemon verifier for host-id-specific claim/report/complete, live token-binding verifier proving another token cannot claim or complete a bound host's request | `go test ./...`, `go test ./internal/server/db`, `npm --prefix web run test:e2e`, `./scripts/verify-operator-workflow.sh`, `./scripts/verify-agent-binary-workflow.sh`, `./scripts/verify-live-agent-token-binding.sh`; full production installed-agent rollout remains environment-specific |
| R8 | Calculate CVSS and match vulnerabilities while distinguishing OS package vulnerabilities from library vulnerabilities. | Verified | CVSS recalculation support, ecosystem-aware affected package index, package type/ecosystem fields on findings, fixed/range evidence checks, inclusive `last_affected`, exclusive `limit`, pre-release handling, and numeric epoch version comparison tests | `go test ./...`, `./scripts/verify-cve-matching-invariants.sh` |
| R9 | Collect from diverse public DBs and merge carefully, preserving only records with sufficient fixed/range evidence for matching. | Implemented | CISA KEV, FIRST EPSS, OSV, NVD, Trivy scripts; OSV ecosystem chunk imports now append/upsert with `replace=false` instead of replacing the whole `osv` source per chunk and defer expensive finalization with `finalize=false` until all OSV chunks finish; OSV conversion preserves complete affected package, reference, and alias evidence instead of truncating large distro/vendor advisories; CVE/source upserts merge existing and incoming `affected_products`/`refs` arrays so later OSV ecosystem chunks do not overwrite package evidence from earlier ecosystems and refreshed category/ecosystem values update existing rows; successful OSV syncs prune rows older than the sync start timestamp so upstream removals do not remain indefinitely; targeted `scripts/sync-trivy-cvedb.sh` refreshes only the Trivy-derived source when that source is stale; source-scoped affected/reference index refresh after finalized bulk imports; security recalculation now merges EPSS columns before CVSS/enrichment/rematch so deferred chunks receive priority data; matchability policy; affected/reference indexes; placeholder rejection; executable rematch test proving missing fixed evidence and non-affected versions do not create CVE DB findings; executable cleanup test proving stale missing-fixed/wrong-ecosystem rematch findings are removed while compatible findings remain; live OSV snapshot has 466608 OSV rows, 1604017 affected-package rows, 208762 matchable rows, no `TEMP-*`/`CVD-*` placeholders, and no stale Android/Azure Linux/openSUSE/openEuler/Mageia rows misclassified as code libraries; live CVE DB quality verifier for source count, matchability, EPSS enrichment, optional required-source freshness, direct DB index invariants, placeholder rejection, affected package evidence, hash-like fixed-version rejection, matchable Packagist sentinel API evidence, reference grouping, and API responsiveness | `go test ./...`, `go test ./internal/server/db`, `bash -n scripts/sync-all-cvedb.sh`, `bash -n scripts/sync-trivy-cvedb.sh`, `./scripts/verify-migrations.sh`, `./scripts/verify-live-cvedb-quality.sh` |
| R10 | Provide one-line installer that downloads required binaries, can deploy as service, and uploads scan results. | Verified for installer paths | `/api/install.sh`, generated one-line installer header-auth downloads and checksum verification, generated installer systemd path/systemctl overrides for safe service verification, `scripts/install-agent.sh`, installer readiness metrics, admin agent-fleet status endpoint for installer readiness plus host version drift, outdated percentage, degraded status, warnings, and recommended actions for missing installer payloads or stale/outdated agents, live installer payload verifier for ready agent/Trivy binaries, valid SHA256 metadata, install-token configuration, and agent version/source commit alignment, cron/download/systemd smoke verification, `BONGSU_AGENT_API_KEY` env fallback so packaged installs do not need agent keys in command arguments, optional `BONGSU_HOST_ID` host identity override, installer-persisted large-host scan controls (`BONGSU_AGENT_SCAN_ROOT`, `BONGSU_AGENT_TRIVY_TIMEOUT_SECONDS`, `BONGSU_AGENT_CONTAINER_TIMEOUT_SECONDS`, `BONGSU_AGENT_SKIP_CONTAINERS`, `BONGSU_AGENT_MAX_CONTAINERS`), real agent binary workflow verifier proving the packaged-style binary can upload inventory and service-style polling can complete requests, live agent token binding verifier for upload/claim/complete authorization | `go test ./internal/server/api`, `./scripts/verify-installer-smoke.sh`, `./scripts/verify-live-installer-payload.sh`, `./scripts/verify-agent-binary-workflow.sh`, `./scripts/verify-live-agent-token-binding.sh`, `go test ./...` |
| R11 | Everything must be sufficiently tested. | Partial | Go unit/source tests, admin-session authorization regression coverage, server scan-report normalization tests for host/container asset context and invalid asset types, executable DB tests for automatic security DB rescan queueing, matchable-only CVE rematch insertion, stale rematch cleanup, latest-SBOM package ontology preservation, stable SLA compliance JSON fields, stable risk report export item arrays, stable RBAC subject/policy list item arrays, RBAC operational status counters, security DB actionable warning/action guidance, stable CVE DB search/affected-package empty-result arrays, health partial affected-index quality fallback, admin security DB status endpoint contract, and admin agent-fleet status endpoint contract; generated and packaged installer safety tests including header-only downloads, checksum mismatch rejection, env-provided agent API keys, cron reinstall replacement, host-id override, and systemd unit generation; Playwright E2E for CVE DB, dashboard partial-failure behavior, force scan, RBAC admin flows, schedules, asset groups, reports, trends, notifications with API payload assertions, rate limiting, OpenAPI route/security coverage; live operator workflow verifier for sessions, health/metrics observability, security DB status, agent fleet status, RBAC status, schedules, asset groups, RBAC list contracts, executive/risk/SLA reports and executive/SLA/risk report export, notifications, OpenAPI, backup dry-run, restore dry-run, agent claim/report/complete; live CVE DB quality verifier for matchable evidence plus empty-result API array contracts; live installer payload verifier for one-line installer binary version/checksum readiness; operations runbook verifier for documented ports, Caddy isolation, release gates, live staging gates, airgap defaults, persisted CVE freshness fields, alert metrics, and packaged runbook-referenced scripts; backup/restore archive verifier for safe regular-file tar entries, required members, duplicates, symlink rejection, sidecar archive checksum rejection, and manifest checksums; real agent binary workflow verifier for two logical hosts, host/container inventory, host/container code-library packages, purl/ecosystem preservation, host-id override, and daemon polling; live RBAC scope verifier for dynamic asset-group policy expansion; live agent token binding verifier for report, claim, and completion authorization; deployment/static/installer/migration/runbook/archive/package-smoke verifiers; static verifier executes both agent and server `--version` metadata; GitHub Actions CI | CI and local verifier suite; broader real multi-host/container fixtures remain useful |
| R12 | Use parallel worktrees/sub-agents where useful; static binaries by default. | Partial | static binary verifier builds `CGO_ENABLED=0` linux/amd64 agent/server binaries, rejects dynamic linkage, executes both `--version` outputs, package script builds release binaries with `-trimpath`, CI static build gate | `./scripts/verify-static-binaries.sh`, `go test ./cmd/server ./cmd/agent`; sub-agent usage is process evidence, not repository evidence |
| R13 | Management server deploys with Docker Compose. | Verified | `deploy/docker-compose.yml`, `deploy/docker-compose.airgap.yml`, server/agent Dockerfiles, safe default verifier, Dockerfile Go base-image version check against `go.mod`, real Docker image builds during package generation | compose config checks, `./scripts/verify-deploy-config.sh` |
| R14 | Product name is `bongsu`, dashboard introduces the name and meaning, and pushes go to `github.com/ziozzang/bongsu` main. | Verified | README, dashboard first-screen `bongsu` heading and `봉수대` meaning text, git remote push history | `npm --prefix web run test:e2e`, `./scripts/verify-live-web-smoke.sh`, `git remote -v`, `git log` |
| R15 | Reject `TEMP-*` and `CVD-*` placeholder vulnerabilities from CVE DB and matching paths. | Verified | migrations `027`, `030`, `035`, `041`, placeholder filters, CVE search API guards, live CVE DB quality verifier with optional direct table checks across CVE rows, affected-package index, and reference keys | `go test ./...`, `./scripts/verify-migrations.sh`, `BONGSU_DB_DSN=... ./scripts/verify-live-cvedb-quality.sh` |
| R16 | EPSS should be columns on matching CVE/advisory rows, not only standalone rows. | Verified | `epss_score`, `epss_percentile` fields, EPSS synchronization and merge stats, live CVE DB quality verifier checking EPSS source rows plus EPSS-enriched non-EPSS CVE/advisory rows through API and direct DB invariants | `go test ./...`, `BONGSU_DB_DSN=... ./scripts/verify-live-cvedb-quality.sh` |
| R17 | Group related advisory records by references such as CVE, Debian, GHSA, vendor keys. | Verified | `cve_reference_keys`, reference-group API, dashboard expansion behavior, live CVE DB quality verifier checking reference group API structure and direct DB multi-source canonical CVE plus vendor/advisory key materialization when `BONGSU_DB_DSN` is set | `go test ./...`, `npm --prefix web run test:e2e`, `BONGSU_DB_DSN=... ./scripts/verify-live-cvedb-quality.sh` |
| R18 | Keep API on 5677 and web on 5678; do not depend on or modify Caddy. | Verified | compose defaults, deploy verifier, local runtime handoff | `./scripts/verify-deploy-config.sh` |

| R19 | Maintain documented, operator-usable API and recovery surfaces as the product grows. | Implemented | embedded and docs OpenAPI 3.0 specs, OpenAPI verifier with route sync plus sensitive operation security checks, backup/restore scripts with archive member validation, regular-file enforcement, optional backup sidecar checksum verification, and manifest checksums, live backup/restore dry-run verifier, backup archive safety verifier, admin security DB status endpoint for sync/freshness/recalculation/index quality plus warnings and recommended actions, admin agent-fleet status endpoint for installer readiness, agent version drift, degraded state, warnings, and recommended actions, live health and admin metrics verifier for security DB revision, recalculation, usable affected/reference index status, health partial affected-index fallback, EPSS enrichment, agent fleet drift, and rescan progress, CHANGELOG, report/trend/notification/admin endpoints | `./scripts/verify-openapi.sh`, `./scripts/verify-backup-restore-archive.sh`, `./scripts/verify-operator-workflow.sh`, `go test ./...` |

## Required Verification Suite

Run this suite before claiming a handoff state is healthy:

```bash
git status --short --branch
./scripts/verify-release-readiness.sh
go test ./...
./scripts/verify-migrations.sh
./scripts/verify-deploy-config.sh
./scripts/verify-requirements-audit.sh
./scripts/verify-operations-runbook.sh
./scripts/verify-cve-matching-invariants.sh
./scripts/verify-openapi.sh
./scripts/verify-backup-restore-archive.sh
./scripts/verify-operator-workflow.sh
./scripts/verify-agent-binary-workflow.sh
./scripts/verify-live-agent-token-binding.sh
./scripts/verify-live-cvedb-quality.sh
./scripts/verify-live-installer-payload.sh
./scripts/verify-live-rbac-scope.sh
./scripts/verify-live-web-smoke.sh
./scripts/verify-package-contents.sh
./scripts/verify-airgap-package-smoke.sh
./scripts/verify-airgap-release-archive.sh <generated-bongsu-archive.tar.gz>
./scripts/verify-airgap-offline-rehearsal.sh <generated-bongsu-archive.tar.gz>
./scripts/verify-installer-smoke.sh
./scripts/verify-static-binaries.sh
npm --prefix web run build
npm --prefix web run test:e2e
BONGSU_DB_PASSWORD=bongsu BONGSU_API_KEY=test-admin-key-0123456789 BONGSU_AGENT_API_KEY=test-agent-key-0123456789 BONGSU_INSTALL_TOKEN=test-install-token-0123456789 docker compose -f deploy/docker-compose.yml config >/tmp/bongsu-compose.out
BONGSU_DB_PASSWORD=bongsu BONGSU_API_KEY=test-admin-key-0123456789 BONGSU_AGENT_API_KEY=test-agent-key-0123456789 BONGSU_INSTALL_TOKEN=test-install-token-0123456789 docker compose -f deploy/docker-compose.airgap.yml config >/tmp/bongsu-airgap-compose.out
git diff --check
```

## Remaining Commercial-Readiness Gaps

- Live browser smoke is automated through `./scripts/verify-live-web-smoke.sh` and currently covers first-screen `bongsu`/`봉수대` product intro, dashboard CVE DB status plus CVE Search, Hosts, Packages, Containers, Scan History, Vulnerabilities, RBAC, Audit Log, Schedules, Asset Groups, Trends, Reports, and Notifications on the deployed `5678` web UI while failing on any `/api/` 5xx response; Hosts now expose force-scan and delete controls, with host deletion covered by API/OpenAPI tests plus live verifier cleanup; repeat browser smoke after larger UI changes and still do a staging visual pass before commercial release.
- Multi-host/container RBAC enforcement now has source-level coverage and a live two-host/two-container viewer-key scope verifier across host, package, container, scan, and scan-request APIs using a dynamic `asset_group` policy such as `team:rbac-allowed`; broader multi-tenant staging fixtures with real installed agents should still be exercised before a commercial release.
- Large imported CVE DB performance and quality are now measured by `./scripts/verify-live-cvedb-quality.sh` against current production-scale snapshots, including direct DB invariant checks when `BONGSU_DB_DSN` is provided for placeholder rejection, affected-package evidence, hash-like fixed-version rejection, EPSS column enrichment, multi-source canonical CVE reference groups, vendor/advisory key materialization, and a conditional OSV Packagist sentinel that requires `phenx/php-svg-lib` package/ecosystem/fixed evidence through the matchable CVE DB search API when a production-scale Packagist feed is present; set `BONGSU_VERIFY_CVEDB_REQUIRE_FRESH_SOURCES=true` for release-candidate and production gates so stale or missing required sources fail the same verifier; targeted Trivy source refresh was live-verified after a stale `trivy` source degraded health; targeted OSV source refresh is now packaged as `./scripts/sync-osv-cvedb.sh` for recovering OSV-only lag without replacing unrelated source rows; OSV append imports were live-verified after fixing the per-ecosystem source replacement bug, OSV CVE/source conflicts now merge affected package evidence instead of overwriting earlier ecosystem chunks, successful OSV syncs now prune source rows older than the sync start timestamp, OSV conversion now preserves full affected/reference/alias evidence instead of truncating large advisories, and OSV chunk finalization is deferred to one post-loop index rebuild/recalculation pass. The current live strict gate passed with 838933 total records, 208762 matchable records, 5 sources, 1604017 affected-package index rows, and zero `TEMP-*`/`CVD-*` placeholders; keep tightening thresholds as larger imports are exercised.
- CVE rematch false-positive controls now have a focused `./scripts/verify-cve-matching-invariants.sh` gate for same-name OS/library collisions, fixed/range evidence, range boundaries, pre-release ordering, and numeric epoch version comparison; extend this suite when new ecosystems or source formats are added.
- Airgap package contents are verified statically, the package script has an end-to-end smoke verifier, and a real generated archive (`bongsu-0.1.0-real-20260604055407.tar.gz`) has been unpacked, verified, loaded, run with packaged airgap compose on isolated ports, and exercised with a 109M security DB bundle import. The package/verifier path now includes the `bongsu-web` image and `postgres:16-alpine` runtime image required by airgap compose, bundle import rebuilds both affected-package and reference-key indexes, and compose defaults allow long-running import responses. A true physically separate offline-network rehearsal remains useful before commercial release.
- Release and handoff verification is consolidated as a release readiness gate in `./scripts/verify-release-readiness.sh`; the operations runbook still should be validated by an operator against a real connected and air-gapped deployment before a commercial release.
- Schedules, asset groups, reports, notification rules, local session login, health/metrics observability, OpenAPI docs, host user/process/port runtime inventory, backup dry-run, restore dry-run, agent claim, agent report ingestion, and scan-request completion now have live API workflow coverage through `./scripts/verify-operator-workflow.sh`; backup archive safety, sidecar transfer checksum verification, and manifest checksum handling are covered by `./scripts/verify-backup-restore-archive.sh`; repeat both against staging and release candidates before a commercial release.
- The real agent binary now has fixture-backed live coverage for two logical host identities, Trivy host OS packages, Trivy host npm libraries, Trivy container OS packages, Trivy container PyPI libraries, osquery packages, purl/ecosystem preservation, container identity, daemon claim, report, and request completion through `./scripts/verify-agent-binary-workflow.sh`; large-host controls bound host Trivy timeout, per-container timeout, scan root, container skip, and max container count through CLI/env/config plus both installer paths, and `./scripts/verify-live-installer-payload.sh` now checks the live one-line installer payload for ready agent/Trivy binaries, valid SHA256 metadata, install-token configuration, and an agent version matching the latest agent/installer source commit while preserving healthy dpkg inventory afterward; live operator/RBAC/token-binding/agent-binary verifiers now delete their own fixture hosts so agent-fleet health is not degraded by synthetic old versions. A broader real multi-host/container deployment rehearsal remains useful before commercial release, especially with mixed Docker density and slow filesystems.
- Host-token binding now has live API coverage through `./scripts/verify-live-agent-token-binding.sh`; keep it in release-candidate runs with `BONGSU_AGENT_HOST_BINDING=true` so stolen or stale tokens cannot report, claim, or complete work for another bound host.
