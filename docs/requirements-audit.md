# Bongsu Requirements Audit

Updated: 2026-06-05 17:32:14 KST

This audit keeps the original product requirements tied to implementation evidence, verification commands, and remaining gaps. It is not a completion claim; the goal remains open until every item has authoritative evidence from code, tests, deployment, and live behavior.

## Status Legend

- `Verified`: covered by committed implementation and an automated verification command.
- `Implemented`: code exists, but the evidence is narrower than the full commercial requirement.
- `Partial`: useful implementation exists, but known product or verification gaps remain.
- `Open`: no sufficient current evidence.

## Requirement Matrix

| ID | Requirement | Current Status | Evidence | Verification |
| --- | --- | --- | --- | --- |
| R1 | Build and continuously update a security database every 6 hours, with import/export for air-gapped transfer. | Verified | `scripts/sync-all-cvedb.sh`, `scripts/export-security-db-bundle.sh`, `scripts/import-security-db-bundle.sh`, `scripts/verify-security-db-bundle-file.sh`, `deploy/docker-compose.yml`, `deploy/docker-compose.airgap.yml`, `docs/architecture.md`; `/api/health` and `/api/admin/security-db/status` expose persisted `security_db_freshness.latest_source`, `latest_last_update`, and `latest_age_seconds` plus `security_db.effective_status`, `effective_source`, and `effective_last_sync` so operators can distinguish a restarted sync manager from an empty or failed CVE DB, `/api/health.security_db_updated_at` is required to align with `security_db_freshness.latest_last_update`, and the live operator workflow requires those effective fields to align with persisted freshness; the security DB sync manager seeds its periodic schedule from persisted CVE source freshness after API restarts so `next_sync` does not drift later than the latest stored source update plus the configured interval; startup affected-package index preparation failures no longer prevent the API listener from starting and instead queue the guarded async affected-index rebuild so health/dashboard surfaces can expose degraded index quality while recovery runs; `/api/admin/security-db/status` returns warnings and recommended actions for stale, missing, failed, timed-out, or not-yet-exported source or raw CVE row updates and exposes `security_db_export` so `./scripts/verify-live-security-db-export-freshness.sh` can fail airgap promotion when source registry updates or raw CVE row updates have not yet been exported; `scripts/export-security-db-bundle.sh` now downloads to a temporary file, runs that live freshness gate with bounded retry for post-response source registry and raw CVE row visibility, and publishes the final bundle plus `.sha256` sidecar only after the gate passes by default; `scripts/verify-security-db-bundle-file.sh` validates the exported bundle sidecar, manifest, source provenance, CVE JSONL checksum/count/parseability, and Trivy DB checksum before airgap transfer, while `scripts/import-security-db-bundle.sh` runs the same local verification before upload by default; package/archive verifiers assert this behavior is shipped, `./scripts/verify-security-db-import-helper-fixtures.sh` proves failed local verification prevents upload, and `./scripts/verify-security-db-export-helper-fixtures.sh` proves delayed freshness succeeds while failed freshness verification does not overwrite an existing final bundle | `./scripts/verify-deploy-config.sh`, `./scripts/verify-live-security-db-schedule.sh`, `./scripts/verify-live-security-db-export-freshness.sh`, `./scripts/verify-security-db-import-helper-fixtures.sh`, `./scripts/verify-security-db-export-helper-fixtures.sh`, `./scripts/verify-security-db-bundle-file.sh <bundle>`, `./scripts/verify-operator-workflow.sh`, `./scripts/verify-package-contents.sh`, compose config checks, `go test ./internal/server/api`, `go test ./internal/server/secdb` |
| R2 | Classify security sources by OS package vs code package/library and match to compatible sources only. | Verified | `docs/vulnerability-matching-rules.md`, `internal/server/db/classify.go` (`compatibleSecurityCandidate` name+ecosystem+category+fixed-evidence+version gating, `compatibleCPECandidate` version-gated CPE-product matching with the no-constraint-never-matches FP guard, `compareVersions` epoch-loss tolerance policy), `internal/server/vercmp/` (dedicated dpkg/rpm/apk/generic comparators with unit tests), `migrations/022_cve_affected_packages.sql`, package ecosystem fields in API models, focused CVE matching invariant verifier for same-name OS/library collisions and ecosystem aliases, executable rematch test that rejects same-name wrong-ecosystem candidates | `go test ./...`, `go test ./internal/server/db`, `go test ./internal/server/vercmp`, `./scripts/verify-cve-matching-invariants.sh` |
| R3 | Collect installed OS/library packages, host metadata, running containers, images, and relationship context, then send SBOM to the server. | Implemented | native scanner is now the agent default (`-scanner native` / `BONGSU_AGENT_SCANNER`): pure-Go dpkg/apk readers plus rpm via the host's `rpm` (`internal/agent/scanner/{dpkg,apk,rpm,scanner}.go`), so the trivy binary is no longer required; language LIBRARY scanning (`scanner/lang.go`) and filesystem-only language RUNTIME detection (`scanner/runtime.go`: pyenv CPython, Node tarballs, JDK `release` files, Go SDK `VERSION`, ruby/php) emitted as `pkg_type=runtime` keyed by CPE product, gated by `-lang-scan-roots`/`-lang-scan-depth`; host facts (`hosts.facts`, migration 055) and container facts (`container_assets.facts`, migration 056) collected natively and returned by `GET /api/hosts/{id}`; `internal/agent/collector`, `cmd/agent`, `internal/agent/reporter`, `internal/shared/models`, server-side scan report asset-context normalization for host/container packages and vulnerabilities, DB package/container/user/process/port persistence, latest-scan host runtime inventory APIs for users, top processes, and listening ports with RBAC scope, live operator workflow verifier that reports deterministic users/processes/ports and then reads them through the host runtime inventory APIs, executable latest-SBOM package query test preserving container/image/target ontology, SBOM export paths in server API, CycloneDX/SPDX ontology preservation tests, server-side Trivy SBOM matching isolated by host/container asset and OS package type so mixed host `deb` plus container `apk` inventories do not fail as one aggregated SBOM, live SBOM export workflow verifier that uploads host OS package, host code-library, container OS package, and container code-library fixture evidence then requires both CycloneDX and SPDX exports to preserve host ID, scan ID, purl, asset type, container ID, image name, image ID, and target context and rejects `server_match`/mixed OS package ingest errors, live SBOM export RBAC verifier proving an export-only asset-group policy can download allowed CycloneDX/SPDX SBOMs while denied host SBOM export fails closed with HTTP 403 and audit evidence, bounded agent scan controls for host scan root, host Trivy timeout, per-container timeout, Docker/osquery/system helper command timeout, container skip, and max container count, live verifier agent report ingestion tied to scan request completion, bounded agent reporter retry handling for network errors, HTTP 429, and HTTP 5xx responses including capped `Retry-After` delays, bounded agent reporter error-response reads so oversized proxy/server errors cannot inflate agent memory, strict JSON report decoding that rejects trailing concatenated payload data, real agent binary workflow verifier with fixture Trivy/osquery/docker collection for two logical hosts, host OS packages, host npm libraries, container OS packages, container PyPI libraries, osquery packages, purl/ecosystem preservation, container runtime name, container ID, image name, image ID, asset ID, host/container isolation, and container inventories, live host-token binding verifier, live RBAC fixture ingestion of distinct allowed/denied hosts and containers, strict real enrolled-agent fleet rollout verifier for online/current/fresh/completed inventory, current security DB revision coverage, and default container label redaction with explicit include_labels opt-in | `go test ./...`, `go test ./internal/server/api`, `go test ./internal/server/db`, `./scripts/verify-operator-workflow.sh`, `./scripts/verify-agent-binary-workflow.sh`, `./scripts/verify-live-sbom-export-workflow.sh`, `./scripts/verify-live-sbom-export-rbac.sh`, `./scripts/verify-live-agent-token-binding.sh`, `./scripts/verify-live-agent-fleet-rollout.sh`, `./scripts/verify-live-rbac-scope.sh`; real fleet deployment fixture remains useful |
| R4 | RBAC by UserID/GroupID for host/container/system access. | Verified | access subject/policy migrations, local admin sessions accepted for admin APIs, viewer-key flow, `/api/admin/rbac-*` handlers, `/api/admin/rbac/status` operational counters for subjects, policies, wildcard policies, orphan policies, resource types, permissions, subject types, and auth configuration visibility for viewer keys plus trusted identity headers and OIDC bearer JWT settings, dashboard RBAC view with auth operational status, dynamic asset-group scope expansion, source-level scope enforcement tests for host/container/image/asset-group policies, live RBAC scope verifier for two-host/two-container viewer-key filtering across hosts, packages, containers, scans, and scan requests via an `asset_group` policy plus fail-closed denied-host filters, live SBOM export RBAC verifier proving an export-only asset-group policy scopes CycloneDX/SPDX exports to the allowed host and makes explicit denied-host SBOM export fail closed with HTTP 403, live vulnerability export RBAC verifier proving an export-only asset-group policy scopes JSON/CSV exports to the allowed host and makes explicit denied-host export fail closed with HTTP 403, live report export RBAC verifier proving risk/executive report exports are scoped to the export asset group and export-only permission does not grant read access to non-export report endpoints, bounded vulnerability export timeout returning 504 with audit metadata before response streaming, trusted reverse-proxy identity headers mapped to `user:`/`group:` RBAC subjects with proxy-CIDR fail-closed checks, trusted admin user/group allowlists, live trusted identity RBAC verification, and live OIDC bearer JWT verification against a temporary JWKS/issuer with issuer/audience/signature/expiration checks plus subject/group/admin mapping while keeping local password auth and API-key admin auth active | `go test ./...`, `npm --prefix web run test:e2e`, `./scripts/verify-operator-workflow.sh`, `./scripts/verify-live-rbac-scope.sh`, `./scripts/verify-live-oidc-rbac.sh`, `./scripts/verify-live-sbom-export-rbac.sh`, `./scripts/verify-live-report-export-rbac.sh`, `./scripts/verify-live-vulnerability-export-rbac.sh`; broader multi-tenant staging fixtures remain useful |
| R5 | Provide a sufficiently good web interface. | Verified | Vite dashboard, first-screen `bongsu`/`봉수대` product intro, CVE DB status card, persisted latest OSV/source update display, OSV ecosystem affected-index freshness summary, admin security DB and agent fleet warnings with recommended operator actions, CVE Search, vulnerability/host/RBAC/audit/admin views, host detail runtime inventory panels for users, top processes, and listening ports, username/password login through the `5678` web proxy, live API/proxy session-auth verifier for login/me/admin/logout/reused-token rejection, schedules, asset groups, trends, reports, notifications, optional-widget failure tolerance, browser workflow coverage for schedules, asset groups, reports, notifications, and operational warning rendering; fixed schedule and asset-group UI/API contract mismatches; live browser verifier for dashboard, CVE Search, Hosts, Packages, Containers, Scan History, Vulnerabilities, RBAC, Audit Log, Schedules, Asset Groups, Trends, Reports, and Notifications routes plus product intro text | `npm --prefix web run build`, `npm --prefix web run test:e2e`, `./scripts/verify-live-session-auth.sh`, `./scripts/verify-live-web-smoke.sh` |
| R6 | Deploy fully in air-gapped environments; update externally, export, import internally. | Verified | airgap compose file, bundle import/export, security DB bundle file verifier, package script, packaged docs, static binary verification, trivy archive validation, package contents verifier, package smoke verifier, offline package rehearsal, generated release archive verifier for required outer `.tar.gz.sha256` sidecars plus inner checksums, required contents, executable/static binaries, Docker image archives for server/web/agent/postgres, loader, and airgap compose invariants; backup, restore, Trivy DB download, and large security DB sync/extract scripts honor `BONGSU_TMPDIR` for large transfer work on hosts with small `/tmp`; real `scripts/package.sh` run produced `bongsu-0.1.0-real-20260604055407.tar.gz` (208M, SHA256 `ff200255584ecc524a1cf6daf387c5a5ab47dfe65a8b804c5bfef44ba45d7900`), archive verification and offline rehearsal passed, the packaged loader loaded server/agent/web/postgres images, packaged compose started on isolated ports `5687/5688`, and a 109M exported security DB bundle imported successfully with `806272` CVE rows, `682665` affected-package rows, `1308724` reference keys, and 100% affected/reference index coverage | `./scripts/verify-deploy-config.sh`, `./scripts/verify-package-contents.sh`, `./scripts/verify-security-db-bundle-file.sh <bundle>`, `./scripts/verify-airgap-archive-checksum-fixtures.sh`, `./scripts/verify-airgap-package-smoke.sh`, `./scripts/verify-airgap-release-archive.sh <archive>`, `./scripts/verify-airgap-offline-rehearsal.sh <archive>`, `./scripts/verify-static-binaries.sh`, compose config checks, isolated package load/compose/import rehearsal |
| R7 | Provide force scan and rematch functions. | Implemented | scan request APIs, dashboard force-scan buttons, asset-group scan trigger, scheduled scans, agent daemon polling/retry, CVE rematch endpoints (`RematchCVEs` OSV path plus `RematchCPE` runtime/CPE path; both run on scan ingest, admin `POST /api/admin/cve-db/rematch`, and the security recalculation pipeline so the recalculate endpoint now also runs CPE rematch), executable CVE rematch test for compatible matchable candidates, executable stale-rematch cleanup test for removing obsolete `cve-db` findings only (with the runtime/CPE exemption that re-checks `compatibleCPECandidate` so valid runtime CVEs are not deleted as stale; batch size `BONGSU_STALE_REMATCH_CLEANUP_BATCH_SIZE`), auto-rescan on security DB changes, executable DB test for security DB update queue accounting/dedupe/revision propagation, live operator workflow verifier for schedule CRUD, asset-group scan trigger, agent claim, agent report, and scan-request completion, live stale scan-request recovery verifier that ages a claimed request in PostgreSQL then verifies stale filtering, admin `/api/scan-requests/requeue-stale`, audit metadata, and re-claimability, live security DB auto-rescan verifier that imports a finalized temporary CVE source and proves recalculation queues a claimable `security-db-update` packages-only request with security DB revision metadata, live CVE rematch workflow verifier that uploads a Packagist fixture SBOM and verifies report-triggered automatic OSV rematch creates `cve-db` findings with package/ecosystem/fixed-version evidence, proves each generated finding is backed by a matching `cve_affected_packages` package/ecosystem/fixed-version row, and checks explicit scan-scoped rematch idempotency, real agent binary daemon verifier for host-id-specific claim/report/complete, agent reporter unit coverage for capped `Retry-After` handling, live token-binding verifier proving another token cannot claim or complete a bound host's request | `go test ./...`, `go test ./internal/server/db`, `npm --prefix web run test:e2e`, `./scripts/verify-operator-workflow.sh`, `./scripts/verify-live-scan-request-recovery.sh`, `./scripts/verify-live-security-db-auto-rescan.sh`, `./scripts/verify-live-cve-rematch-workflow.sh`, `./scripts/verify-agent-binary-workflow.sh`, `./scripts/verify-live-agent-token-binding.sh`; full production installed-agent rollout remains environment-specific |
| R8 | Calculate CVSS and match vulnerabilities while distinguishing OS package vulnerabilities from library vulnerabilities. | Verified | CVSS recalculation support, ecosystem-aware affected package index, package type/ecosystem fields on findings, fixed/range evidence checks, inclusive `last_affected`, exclusive `limit`, pre-release handling, and numeric epoch version comparison tests | `go test ./...`, `./scripts/verify-cve-matching-invariants.sh` |
| R9 | Collect from diverse public DBs and merge carefully, preserving only records with sufficient fixed/range evidence for matching. | Implemented | CISA KEV, FIRST EPSS, OSV, NVD, Trivy scripts; large source download/extract/sync scripts honor `BONGSU_TMPDIR`; OSV downloads preflight temporary free space before download and extraction with tunable `BONGSU_OSV_MIN_FREE_BYTES` so large distro snapshots fail closed with a clear recovery action instead of truncating mid-extraction; OSV ecosystem chunks append/upsert with `replace=false` and deferred `finalize=false`, then prune stale rows only after all selected chunks succeed; connected sync scripts preflight the server's advertised affected/reference rebuild timeout through `/api/admin/security-db/status` and fail early when it is below `BONGSU_CVE_INDEX_REBUILD_MIN_SERVER_TIMEOUT_SECONDS`; full OSV syncs now run the strict upstream freshness verifier after import/rebuild/recalculate, while partial `BONGSU_OSV_ECOSYSTEMS` refreshes run ecosystem-scoped freshness verification when `BONGSU_DB_DSN` is available and otherwise emit an explicit skip because aggregate OSV freshness is intentionally not promoted; OSV conversion preserves affected package, reference, alias, fixed-version, and range evidence, skips CGA-only Chainguard advisories without CVE aliases before upload while logging skipped vs importable counts, and keeps aggregate source registry freshness unchanged after partial ecosystem syncs; source upserts merge `affected_products`/`refs` so distro chunks cannot overwrite package registry evidence; partial `BONGSU_OSV_ECOSYSTEMS` skips source-wide prune; source-scoped affected/reference index refresh, EPSS merge before CVSS/enrichment/rematch, OSV ecosystem freshness metrics, matchability policy, placeholder rejection, and stale-rematch cleanup are covered; fixed-version evidence now rejects literal `0` placeholder fixed versions as non-actionable so OSV `introduced: 0` or upstream placeholder rows cannot become matchable findings; live OSV snapshot after targeted Wolfi freshness recovery has 466906 OSV rows, 1605108 affected-package rows, 208843 matchable rows, no `fixed_version='0'` affected rows, no `TEMP-*`/`CVD-*` placeholders, and no stale Android/Azure Linux/openSUSE/openEuler/Mageia rows misclassified as code libraries | `go test ./...`, `go test ./internal/server/db`, `bash -n scripts/sync-all-cvedb.sh`, `bash -n scripts/sync-osv-cvedb.sh`, `bash -n scripts/sync-trivy-cvedb.sh`, `./scripts/verify-migrations.sh`, `./scripts/verify-live-cvedb-quality.sh` |
| R10 | Provide one-line installer that downloads required binaries, can deploy as service, and uploads scan results. | Verified for installer paths | `/api/install.sh`, generated one-line installer header-auth downloads and checksum verification, generated installer systemd path/systemctl overrides for safe service verification, `scripts/install-agent.sh`, installer readiness metrics, admin agent-fleet status endpoint for installer readiness plus host version drift, outdated percentage, degraded status, warnings, and recommended actions for missing installer payloads or stale/outdated agents, live installer payload verifier for ready agent/Trivy binaries, valid SHA256 metadata, install-token configuration, and agent version/source commit alignment, live install-script verifier for actual `/api/install.sh` and `/api/downloads/*` endpoint authentication, no query-token leakage, checksum headers, and matching downloaded bytes, cron/download/systemd smoke verification, `BONGSU_AGENT_API_KEY` env fallback so packaged installs do not need agent keys in command arguments, optional `BONGSU_HOST_ID` host identity override, installer-persisted large-host scan controls (`BONGSU_AGENT_SCAN_ROOT`, `BONGSU_AGENT_TRIVY_TIMEOUT_SECONDS`, `BONGSU_AGENT_CONTAINER_TIMEOUT_SECONDS`, `BONGSU_AGENT_COMMAND_TIMEOUT_SECONDS`, `BONGSU_AGENT_SKIP_CONTAINERS`, `BONGSU_AGENT_MAX_CONTAINERS`), real agent binary workflow verifier proving the packaged-style binary can upload inventory and service-style polling can complete requests, live agent token binding verifier for upload/claim/complete authorization | `go test ./internal/server/api`, `./scripts/verify-installer-smoke.sh`, `./scripts/verify-live-installer-payload.sh`, `./scripts/verify-live-install-script.sh`, `./scripts/verify-agent-binary-workflow.sh`, `./scripts/verify-live-agent-token-binding.sh`, `go test ./...` |
| R11 | Everything must be sufficiently tested. | Partial | Go unit/source tests, trusted identity header RBAC subject/admin/proxy-CIDR regression coverage, live trusted identity RBAC verifier coverage, live OIDC bearer RBAC verifier coverage with generated RS256 JWTs/JWKS and wrong-audience/expired-token rejection, admin-session authorization regression coverage, server scan-report normalization tests for host/container asset context and invalid asset types, executable DB tests for automatic security DB rescan queueing, matchable-only CVE rematch insertion, stale rematch cleanup, latest-SBOM package ontology preservation, stable SLA compliance JSON fields, stable risk report export item arrays, stable RBAC subject/policy list item arrays, RBAC operational status counters plus trusted identity auth visibility, security DB actionable warning/action guidance, stable CVE DB search/affected-package empty-result arrays, health indexed-only CVE index summaries plus partial affected-index quality summary coverage for detailed endpoints, admin security DB status endpoint contract, admin agent-fleet status endpoint contract, admin Prometheus agent fleet operational metrics, and admin Prometheus OSV ecosystem freshness metric source checks; security DB scheduler tests cover persisted-freshness seeding after restarts; notification rule webhook tests cover HMAC signing, transient 5xx retry, HTTP 4xx non-retry, and bounded retry env parsing; generated and packaged installer safety tests including header-only downloads, checksum mismatch rejection, env-provided agent API keys, cron reinstall replacement, host-id override, and systemd unit generation; agent reporter tests cover bounded retries, non-retryable 4xx responses, capped `Retry-After`, and bounded error bodies; Playwright E2E for CVE DB, dashboard partial-failure behavior, force scan, RBAC admin flows, schedules, asset groups, reports, trends, notifications with API payload assertions, rate limiting, OpenAPI route/security coverage; live operator workflow verifier for sessions, health/metrics observability, security DB status, agent fleet status, RBAC status, schedules, asset groups, RBAC list contracts, executive/risk/SLA reports and executive/SLA/risk report export, audited retention dry-run, signed notification webhook retry, OpenAPI, backup dry-run, restore dry-run, agent claim/report/complete; live destructive retention prune verifier for fixture-only old scans, scan-scoped child inventories, terminal scan requests, audit rows, latest usable scan preservation, old running scan preservation, pending request preservation, and prune audit metadata; live stale scan-request recovery verifier for stuck claimed work requeue and audit evidence; live vulnerability triage verifier for required reasons, accepted-risk filters, stats, metrics, exports, audit logs, and expiry behavior; live CVE DB quality verifier for matchable evidence plus empty-result API array contracts; live CVE DB concurrency verifier for simultaneous forced stats refreshes, security DB status, admin metrics, healthy CVE DB quality, and absence of new PostgreSQL shared-memory errors; live security DB schedule verifier for the persisted-update plus interval cadence; live server build verifier for API binary source-commit readiness; live installer payload verifier for one-line installer binary version/checksum readiness; operations runbook verifier for documented ports, Caddy isolation, release gates, live staging gates, airgap defaults, persisted CVE freshness fields, OSV ecosystem and agent fleet alert metrics, release readiness JSON evidence reports with release archive SHA-256/sidecar match evidence, and packaged runbook-referenced scripts; backup/restore archive verifier now exercises `backup.sh` output with a pg_dump stub, sidecar checksum, manifest database/trivy fields, restore dry-run acceptance, safe regular-file tar entries, required members, duplicates, symlink rejection, sidecar archive checksum rejection, and manifest checksums; real agent binary workflow verifier for two logical hosts, host/container inventory, host/container code-library packages, container runtime name, container ID, image name, image ID, asset ID, purl/ecosystem preservation, host-id override, container isolation, and daemon polling; live RBAC scope verifier for dynamic asset-group scope expansion; live report export RBAC verifier for risk/executive export scoping and export-only read denial; live agent token binding verifier for report, claim, and completion authorization; live fixture cleanup verifier statically enforces cleanup traps plus host removal for verifier-created `host-*` fixtures, RBAC subject/policy removal for verifier-created access fixtures, and scan-request cancellation/removal for verifier-created queue fixtures, so release checks do not leave synthetic hosts degrading agent-fleet health, stale access policies polluting authorization tests, or abandoned scan requests inflating force-scan/rescan backlogs; deployment/static/installer/migration/runbook/archive/package-smoke verifiers; static verifier executes both agent and server `--version` metadata; GitHub Actions CI runs the consolidated release readiness gate and uploads the `bongsu-ci-release-readiness` JSON evidence artifact | CI and local verifier suite; broader real multi-host/container fixtures remain useful |
| R12 | Use parallel worktrees/sub-agents where useful; static binaries by default. | Partial | static binary verifier builds `CGO_ENABLED=0` linux/amd64 agent/server binaries, rejects dynamic linkage, executes both `--version` outputs, package script builds release binaries with `-trimpath`, CI static build gate | `./scripts/verify-static-binaries.sh`, `go test ./cmd/server ./cmd/agent`; sub-agent usage is process evidence, not repository evidence |
| R13 | Management server deploys with Docker Compose. | Verified | `deploy/docker-compose.yml`, `deploy/docker-compose.airgap.yml`, server/agent Dockerfiles, safe default verifier, Dockerfile Go base-image version check against `go.mod`, real Docker image builds during package generation | compose config checks, `./scripts/verify-deploy-config.sh` |
| R14 | Product name is `bongsu`, dashboard introduces the name and meaning, and pushes go to `github.com/ziozzang/bongsu` main. | Verified | README, dashboard first-screen `bongsu` heading and `봉수대` meaning text, git remote push history | `npm --prefix web run test:e2e`, `./scripts/verify-live-web-smoke.sh`, `git remote -v`, `git log` |
| R15 | Reject `TEMP-*` and `CVD-*` placeholder vulnerabilities from CVE DB and matching paths. | Verified | migrations `027`, `030`, `035`, `041`, placeholder filters, CVE search API guards, live CVE DB quality verifier with optional direct table checks across CVE rows, affected-package index, and reference keys | `go test ./...`, `./scripts/verify-migrations.sh`, `BONGSU_DB_DSN=... ./scripts/verify-live-cvedb-quality.sh` |
| R16 | EPSS should be columns on matching CVE/advisory rows, not only standalone rows. | Verified | `epss_score`, `epss_percentile` fields, EPSS synchronization and merge stats, live CVE DB quality verifier checking EPSS source rows plus EPSS-enriched non-EPSS CVE/advisory rows through API and direct DB invariants | `go test ./...`, `BONGSU_DB_DSN=... ./scripts/verify-live-cvedb-quality.sh` |
| R17 | Group related advisory records by references such as CVE, Debian, GHSA, vendor keys. | Verified | `cve_reference_keys`, reference-group API, dashboard expansion behavior, live CVE DB quality verifier checking reference group API structure and direct DB multi-source canonical CVE plus vendor/advisory key materialization when `BONGSU_DB_DSN` is set | `go test ./...`, `npm --prefix web run test:e2e`, `BONGSU_DB_DSN=... ./scripts/verify-live-cvedb-quality.sh` |
| R18 | Keep API on 5677 and web on 5678; do not depend on or modify Caddy. | Verified | compose defaults, deploy verifier, local runtime handoff | `./scripts/verify-deploy-config.sh` |

| R19 | Maintain documented, operator-usable API and recovery surfaces as the product grows. | Implemented | embedded and docs OpenAPI 3.0 specs, OpenAPI verifier with route sync plus sensitive operation security checks, backup/restore scripts with archive member validation, regular-file enforcement, optional backup sidecar checksum verification, manifest checksums, and backup self-validation through restore dry-run, live backup/restore dry-run verifier, backup archive safety verifier, admin security DB status endpoint for sync/freshness/recalculation/index quality plus warnings and recommended actions, admin RBAC status endpoint for auth configuration visibility, admin agent-fleet status endpoint for installer readiness, agent version drift, degraded state, warnings, and recommended actions, signed/retried notification rule webhook delivery logs, live health and admin metrics verifier for effective persisted security DB status/source/last-sync metrics (`bongsu_security_db_effective_status`, `bongsu_security_db_effective_source_info`, `bongsu_security_db_effective_last_sync_timestamp_seconds`, `bongsu_security_db_sync_persisted_last_success_timestamp_seconds`, `bongsu_security_db_effective_age_seconds`), security DB revision or revision-error state, recalculation, usable affected/reference index status, health partial affected-index fallback, security DB schedule cadence, EPSS enrichment, agent fleet drift/degraded/warning/outdated percentage state, rescan progress, and concurrent CVE DB observability under operator load, live server build verifier for API binary source-commit drift, CHANGELOG, report/trend/notification/admin endpoints | `./scripts/verify-openapi.sh`, `./scripts/verify-backup-restore-archive.sh`, `./scripts/verify-live-security-db-schedule.sh`, `./scripts/verify-live-cvedb-concurrency.sh`, `./scripts/verify-live-server-build.sh`, `./scripts/verify-operator-workflow.sh`, `go test ./...` |

## Required Verification Suite

Run this suite before claiming a handoff state is healthy:

```bash
git status --short --branch
./scripts/verify-release-readiness.sh
BONGSU_RELEASE_READINESS_REPORT=/tmp/bongsu-release-readiness.json ./scripts/verify-release-readiness.sh
BONGSU_DB_DSN="$BONGSU_DB_DSN" BONGSU_RELEASE_READINESS_LIVE=true ./scripts/verify-release-readiness.sh
go test ./...
./scripts/verify-migrations.sh
./scripts/verify-deploy-config.sh
./scripts/verify-requirements-audit.sh
./scripts/verify-operations-runbook.sh
./scripts/verify-cve-matching-invariants.sh
./scripts/verify-openapi.sh
./scripts/verify-backup-restore-archive.sh
./scripts/verify-operator-workflow.sh
./scripts/verify-agent-binary-workflow.sh
./scripts/verify-live-agent-token-binding.sh
./scripts/verify-live-agent-fleet-rollout.sh
./scripts/verify-live-cvedb-quality.sh
./scripts/verify-live-cve-rematch-workflow.sh
./scripts/verify-live-vulnerability-triage.sh
./scripts/verify-live-installer-payload.sh
./scripts/verify-live-install-script.sh
./scripts/verify-live-rbac-scope.sh
./scripts/verify-live-fixture-cleanup.sh
./scripts/verify-live-report-export-rbac.sh
./scripts/verify-live-sbom-export-rbac.sh
./scripts/verify-live-sbom-export-workflow.sh
./scripts/verify-live-scan-request-recovery.sh
./scripts/verify-live-security-db-auto-rescan.sh
./scripts/verify-live-retention-prune.sh
./scripts/verify-live-security-db-schedule.sh
./scripts/verify-live-security-db-export-freshness.sh
./scripts/verify-security-db-bundle-file-fixtures.sh
./scripts/verify-security-db-import-helper-fixtures.sh
./scripts/verify-security-db-export-helper-fixtures.sh
./scripts/verify-security-db-export-freshness-fixtures.sh
./scripts/verify-live-server-build.sh
./scripts/verify-live-session-auth.sh
./scripts/verify-live-oidc-rbac.sh
./scripts/verify-live-trusted-identity-rbac.sh
./scripts/verify-live-vulnerability-export-rbac.sh
./scripts/verify-live-web-smoke.sh
./scripts/verify-package-contents.sh
./scripts/verify-airgap-archive-checksum-fixtures.sh
./scripts/verify-airgap-package-smoke.sh
./scripts/verify-airgap-release-archive.sh <generated-bongsu-archive.tar.gz>
./scripts/verify-airgap-offline-rehearsal.sh <generated-bongsu-archive.tar.gz>
./scripts/verify-installer-smoke.sh
./scripts/verify-static-binaries.sh
npm --prefix web run build
npm --prefix web run test:e2e
BONGSU_DB_PASSWORD=release-db-password-0123456789 BONGSU_API_KEY=release-admin-secret-0123456789 BONGSU_AGENT_API_KEY=release-agent-secret-0123456789 BONGSU_INSTALL_TOKEN=release-install-secret-0123456789 docker compose -f deploy/docker-compose.yml config >/tmp/bongsu-compose.out
BONGSU_DB_PASSWORD=release-db-password-0123456789 BONGSU_API_KEY=release-admin-secret-0123456789 BONGSU_AGENT_API_KEY=release-agent-secret-0123456789 BONGSU_INSTALL_TOKEN=release-install-secret-0123456789 docker compose -f deploy/docker-compose.airgap.yml config >/tmp/bongsu-airgap-compose.out
git diff --check
```

## Remaining Commercial-Readiness Gaps

- Live browser smoke is automated through `./scripts/verify-live-web-smoke.sh` and currently covers first-screen `bongsu`/`봉수대` product intro, dashboard CVE DB status plus CVE Search, Hosts, Packages, Containers, Scan History, Vulnerabilities, RBAC, Audit Log, Schedules, Asset Groups, Trends, Reports, and Notifications on the deployed `5678` web UI while failing on any `/api/` 5xx response; `./scripts/verify-live-session-auth.sh` separately checks username/password session login, bearer-token admin access, logout, reused-token rejection, and the web proxy login path without needing a full browser run; Hosts now expose force-scan and delete controls, with host deletion covered by API/OpenAPI tests plus live verifier cleanup; repeat browser smoke after larger UI changes and still do a staging visual pass before commercial release.
- Multi-host/container RBAC enforcement now has source-level coverage and a live two-host/two-container viewer-key scope verifier across host, package, container, scan, and scan-request APIs using a dynamic `asset_group` policy such as `team:rbac-allowed`; `./scripts/verify-live-sbom-export-rbac.sh` verifies the high-risk SBOM export surface with an export-only asset-group policy, allowed/denied fixture hosts, CycloneDX and SPDX export filtering, explicit denied-host 403 behavior, and `sbom.export` audit evidence; `./scripts/verify-live-vulnerability-export-rbac.sh` additionally verifies the vulnerability export surface with an export-only asset-group policy, allowed/denied fixture vulnerabilities, JSON and CSV export filtering, and explicit denied-host 403 behavior; `./scripts/verify-live-report-export-rbac.sh` covers the fleet summary report export surface with allowed/denied fixture findings, risk/executive export scoping, and a fail-closed check that export-only permission does not grant read access to non-export report endpoints. `./scripts/verify-live-oidc-rbac.sh` starts an isolated temporary API process with generated RS256 OIDC tokens and JWKS so SSO bearer-token issuer/audience/signature/expiration checks, viewer vs admin group mapping, and local admin fallback are verified without restarting the main `5677` API. Broader multi-tenant staging fixtures with real installed agents should still be exercised before a commercial release.
- Large imported CVE DB performance and quality are now measured by `./scripts/verify-live-cvedb-quality.sh` against current production-scale snapshots, including direct DB invariant checks when `BONGSU_DB_DSN` is provided for placeholder rejection, affected-package evidence, non-version fixed-version rejection for URL/package URL/git URL/branch/hash-like values and literal `0` placeholder fixed versions, EPSS column enrichment, multi-source canonical CVE reference groups, vendor/advisory key materialization, `/api/health` freshness alignment with `security_sources`, deferred `finalize=false` OSV chunk imports not promoting aggregate OSV freshness, and a conditional OSV Packagist sentinel that requires `phenx/php-svg-lib` package/ecosystem/fixed evidence through the matchable CVE DB search API when a production-scale Packagist feed is present; set `BONGSU_VERIFY_CVEDB_REQUIRE_FRESH_SOURCES=true` for release-candidate and production gates so stale or missing required sources fail the same verifier, and set `BONGSU_VERIFY_CVEDB_REQUIRE_OSV_UPSTREAM_FRESHNESS=true` in connected live gates so OSV sentinel ecosystems fail when upstream `all.zip` `Last-Modified` is newer than the DB-backed ecosystem affected index, or the aggregate local OSV source when DB access is unavailable, beyond the one-hour default `BONGSU_VERIFY_CVEDB_OSV_UPSTREAM_GRACE_SECONDS=3600`; when `BONGSU_DB_DSN` is available, the same gate also verifies each sentinel ecosystem has matchable affected-package rows whose own `updated_at` timestamp is fresh enough, which catches partial OSV chunk lag without requiring aggregate `osv` source timestamp promotion; targeted Trivy source refresh was live-verified after a stale `trivy` source degraded health; targeted OSV source refresh is now packaged as `./scripts/sync-osv-cvedb.sh` for recovering OSV lag without replacing unrelated source rows, and both full sync scripts now call the strict OSV upstream freshness verifier after successful import/rebuild/recalculate. Partial `BONGSU_OSV_ECOSYSTEMS` overrides skip source-wide stale prune so a narrow refresh cannot delete ecosystems it did not import, and their post-sync freshness verifier runs only with `BONGSU_DB_DSN` so the check remains ecosystem-scoped without promoting aggregate OSV freshness; OSV append imports were live-verified after fixing the per-ecosystem source replacement bug, OSV CVE/source conflicts now merge affected package evidence instead of overwriting earlier ecosystem chunks, successful full OSV syncs now prune source rows older than the sync start timestamp, OSV conversion now preserves full affected/reference/alias evidence instead of truncating large advisories, and OSV chunk finalization is deferred to one post-loop index rebuild/recalculation pass. Affected-package and reference-key index rebuild timeout budgets are now separated from short health/detail lookup budgets, default to `900` seconds, and are exposed in admin status so production OSV finalization is not cancelled by short liveness settings; connected sync scripts now fail early when the running server advertises a rebuild timeout below the configured minimum. `security_db_revision` now uses a short success-only in-process cache and shared in-flight query so health, metrics, export, and stats calls do not repeatedly run the same large source/index aggregation between DB changes. The current live strict gate passed with OSV upstream freshness enabled, direct DB invariants enabled, 961500 total records, 208843 matchable records, 5 sources, zero `TEMP-*`/`CVD-*` placeholders, zero OSV `fixed_version='0'` affected-package rows, affected package rows carrying package/ecosystem/fixed evidence, no orphan affected/reference rows, EPSS columns on non-EPSS advisory rows, and multi-source canonical/vendor reference groups; the refreshed airgap bundle `dist/bongsu-security-db-bundle-20260605-osv-wolfi-refresh.tar.gz` was exported after the targeted Wolfi OSV refresh, passed export freshness verification, has SHA-256 `301b57c8af997608712c57115178580e3b16f685c6e9036cf6fc6fc8f7105919`, and verifies as security DB revision `065b9c09f0b73ae6` with 961500 CVE records; keep tightening thresholds as larger imports are exercised.
- CVE rematch false-positive controls now have a focused `./scripts/verify-cve-matching-invariants.sh` gate for same-name OS/library collisions, fixed/range evidence, range boundaries, pre-release ordering, and numeric epoch version comparison; `./scripts/verify-live-cve-rematch-workflow.sh` now proves the live API can ingest a package-only Packagist SBOM, automatically create `cve-db` findings from OSV affected-package evidence during report handling, keep an explicit scan-scoped rematch idempotent, and directly verify every generated `cve-db` finding is backed by a matching `cve_affected_packages` row for vulnerability ID, package name, normalized ecosystem, and fixed version. `./scripts/verify-live-vulnerability-triage.sh` proves the live triage lifecycle for suppressing statuses, required reasons, accepted-risk filters, stats, Prometheus metrics, JSON/CSV exports, audit logs, and expiry reverting a finding to `open`. `./scripts/verify-live-security-db-auto-rescan.sh` separately proves that a finalized CVE DB update triggers recalculation, emits `security_db.auto_rescan` audit metadata, and leaves a claimable packages-only `security-db-update` request for an eligible host. Extend these suites when new ecosystems or source formats are added.
- Airgap package contents are verified statically, the package script has an end-to-end smoke verifier, and a real generated archive (`bongsu-0.1.0-real-20260604055407.tar.gz`) has been unpacked, verified, loaded, run with packaged airgap compose on isolated ports, and exercised with a 109M security DB bundle import. The package/verifier path now includes the `bongsu-web` image and `postgres:16-alpine` runtime image required by airgap compose, bundle import rebuilds both affected-package and reference-key indexes, and compose defaults allow long-running import responses. `./scripts/verify-airgap-offline-rehearsal.sh` now re-exports its rehearsal `.env` before rendering packaged compose so live verifier credentials such as `test-admin-key` cannot override the safe airgap fixture secrets through Docker Compose environment precedence. A true physically separate offline-network rehearsal remains useful before commercial release.
- Release and handoff verification is consolidated as a release readiness gate in `./scripts/verify-release-readiness.sh`; the live release gate now checks API server build metadata against the latest server/runtime source commit before checking installer payloads, one-line installer/download endpoint authentication and checksums, security DB schedule cadence from persisted source freshness, operational workflows, destructive retention prune safety, and direct PostgreSQL CVE DB invariants by default. The deploy-config subgate is isolated from live runtime overrides such as `BONGSU_ALLOW_WEAK_SECRETS=true`, so connected/airgap compose safe defaults are verified even when the current API process is running with local test credentials. `BONGSU_RELEASE_READINESS_REPORT` persists a JSON evidence report with selected options, git commit, gate commands, exit codes, timestamps, durations, and failed gate count so CI, release archives, and handoff notes can retain auditable verification evidence. The DB-backed live gate now passed with heavy web/package gates enabled, `gate_count=49`, `failed_gate_count=0`, `live=true`, `require_db=true`, and `skip_heavy=false` in `/tmp/bongsu-release-readiness-live-heavy-5cb14c4-final.json`; report-ingest-heavy live verifiers default to a 60 second curl budget so production-scale CVE DB enrichment does not fail release evidence collection at a 20 second client cutoff.
- Schedules, asset groups, reports, notification rules, signed notification rule webhook retry, local session login, health/metrics observability, OpenAPI docs, audited retention dry-run with cutoff timestamps and numeric blast-radius counters, host user/process/port runtime inventory, backup dry-run, restore dry-run, agent claim, agent report ingestion, and scan-request completion now have live API workflow coverage through `./scripts/verify-operator-workflow.sh`; destructive retention prune is separately covered by `./scripts/verify-live-retention-prune.sh`, which aborts if non-fixture old data would be touched and then proves only synthetic terminal scan/request/audit rows are pruned while current inventory, running scans, and pending requests survive; generated backup archive safety, sidecar transfer checksum verification, backup manifest fields, restore dry-run acceptance, and malformed archive rejection are covered by `./scripts/verify-backup-restore-archive.sh`; repeat both against staging and release candidates before a commercial release.
- Agent fleet status now includes stale scan-request counts and current security DB stale rescan counts, and degrades with explicit recommended actions when online or recently seen agents are not claiming pending scan work. This separates simple version drift from force-scan polling failure and gives operators a direct recovery path before automatic rescan backlogs silently accumulate.
- The real agent binary now has fixture-backed live coverage for two logical host identities, Trivy host OS packages, Trivy host npm libraries, Trivy container OS packages, Trivy container PyPI libraries, osquery packages, purl/ecosystem preservation, container runtime name, container ID, image name, image ID, package asset ID, host/container isolation, daemon claim, report, and request completion through `./scripts/verify-agent-binary-workflow.sh`; `./scripts/verify-live-sbom-export-workflow.sh` separately verifies that a live reported host/container inventory can be exported as CycloneDX and SPDX without losing host ID, scan ID, purl, asset type, container ID, image name, image ID, or target context, while `./scripts/verify-live-sbom-export-rbac.sh` proves export-only viewer scope cannot download another host's SBOM; large-host controls bound host Trivy timeout, per-container timeout, scan root, container skip, and max container count through CLI/env/config plus both installer paths, `./scripts/verify-live-installer-payload.sh` checks the live one-line installer payload for ready agent/Trivy binaries, valid SHA256 metadata, install-token configuration, and an agent version matching the latest agent/installer source commit, and `./scripts/verify-live-install-script.sh` checks the actual `/api/install.sh` and `/api/downloads/*` enrollment URLs for header-only authentication, no query-token leakage, checksum headers, and matching downloaded bytes while preserving healthy dpkg inventory afterward; live operator/RBAC/token-binding/agent-binary/SBOM-export verifiers now delete their own fixture hosts so agent-fleet health is not degraded by synthetic old versions. `./scripts/verify-live-agent-fleet-rollout.sh` separately checks real enrolled hosts, not fixtures, and fails commercial rollout readiness on stale/offline/outdated agents, degraded latest inventories, pending scan backlog, incomplete current security DB revision coverage, or default container label exposure without explicit `include_labels=true` opt-in. A broader real multi-host/container deployment rehearsal remains useful before commercial release, especially with mixed Docker density and slow filesystems.
- Host-token binding now has live API coverage through `./scripts/verify-live-agent-token-binding.sh`; keep it in release-candidate runs with `BONGSU_AGENT_HOST_BINDING=true` so stolen or stale tokens cannot report, claim, or complete work for another bound host.
- False-positive reduction is now driven by the dedicated `internal/server/vercmp` per-ecosystem comparators (dpkg `verrevcmp` incl. tilde, `rpmvercmp` incl. tilde/caret/digit-over-alpha, apk suffix ordering, generic semver/PEP 440) plus the `compareVersions` epoch-loss tolerance policy that strips an advisory epoch the installed version lacks (the dominant image/container FP source, e.g. the vim `2:9.0.x` case), and the CPE matcher's no-constraint-never-matches guard. Covered by `go test ./internal/server/vercmp` and `./scripts/verify-cve-matching-invariants.sh`; keep extending fixtures as new ecosystems land.
- Per-finding assignee/triage ownership shipped: each triage row carries an `assignee`, `BONGSU_AUTO_ASSIGN_BY_OWNER` (default true) defaults a host's findings to the host owner via `ON CONFLICT DO NOTHING` (never overwriting human triage), and the vulnerability list/export filter by `assignee` (incl. `unassigned`). Covered by `./scripts/verify-live-vulnerability-triage.sh`.
- Notification channels now include `email` (SMTP via `BONGSU_SMTP_*`, validated at rule-create time) alongside `webhook` and `log`, and the trigger taxonomy added `scan.failed` (migration 057) so degraded/failed scans alert. Webhook signing/retry has live coverage; an end-to-end live email-delivery verifier against a real SMTP sink remains a useful future gap.
- Search/filtering coverage expanded: `GET /api/packages` (name, version, version_like, ecosystem, arch, image, asset_type, has_vulns, min_cvss), `GET /api/vulnerabilities` (ecosystem, pkg_type, vuln_id, max_cvss, has_fix, assignee), `GET /api/hosts` (owner, team, environment, criticality, os, q), and the CVE→assets reverse lookup `GET /api/vulnerabilities/affected-assets`. These are documented in `docs/openapi.yaml`; browser/API smoke covers the surfaces but exhaustive per-param assertions remain a useful gap.
